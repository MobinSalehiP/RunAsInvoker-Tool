# 🔧 Windows Run-As-Invoker Bypass Tool

This repository contains a simple batch script and a text guide that allow you to start Windows applications **without requiring administrator privileges**, by using the `__compat_layer=RunAsInvoker` method.

This method does **not** hack, modify, or exploit the system.  
It simply forces the application to run with the same privileges as the current user (bypassing UAC elevation prompts when possible).

---

## 📂 Files

### `BYPASS.bat`
A batch script that automatically applies:
