# ⚡ RunAsInvoker Tool

A simple tool that allows you to run Windows applications **without UAC prompts**, using the built-in `RunAsInvoker` compatibility layer.  
This does *not* elevate privileges — it simply forces the program to run with the same permissions as the current user.

---

## 🚀 Features
- Run apps **without triggering UAC elevation**
- Uses Windows native `RunAsInvoker`
- No installation required
- Includes BAT script + manual CMD method
- Works on all modern Windows versions

---